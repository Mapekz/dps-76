# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local-only extraction report (embeds an absolute ESM path)
src/data/*/generated/_meta.json

# Regenerable per-run UX critiques from the impeccable skill (design.json and
# live/ config stay tracked — critique/ has no rotation, one ~16K file/run)
.impeccable/critique/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
