// PTS enemies — currently identical to Live (single ESM; no PTS dump yet).
// When a PTS patch diverges, replace this re-export with PTS-specific data.
export { enemies, enemyMutations, legendaryRankModifiers } from '../live/enemies';
